package com.esprit.tunicare.repositories;

import com.esprit.tunicare.entities.patient_hospitalise;
import org.springframework.data.jpa.repository.JpaRepository;

public interface patient_hospitaliseRepository extends JpaRepository<Long, patient_hospitalise> {
}
